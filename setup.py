from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Machine learning to analyze, model and classify time-series and signals',
    author='vinod',
    license='',
)
